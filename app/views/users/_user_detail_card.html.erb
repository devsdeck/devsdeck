<div class="card">

    <div class="image">
      <%= image_tag(avatar_url(@user)) %>
    </div>
    <div class="content">
      <div class="header">
        <div><%= @user.email %></div>
      </div>
      <div class="description">
        <%= @user.description %>
      </div>
    </div>
    <div class="extra content center aligned">
      <span>
        <%= link_to "Edit Profile", edit_user_path(@user) %>
      </span>
    </div>
    <div class="extra content center aligned">
      <%= render 'statistic', total: @cards.size, label: 'Cards'%>
      <%= render 'statistic', total: @liked.size, label: 'Likes'%>
   </div>
    </div>
