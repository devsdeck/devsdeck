<div class="ui two stackable cards">
  <% @cards.each do |card| %>
  <div class="green ui fluid card">
    <div class="content">
      <%= link_to card.name, card_path(card), class: 'header'%>
      <div class="meta">
        <span class="date"><%= link_to card.user.email, card.user %> <i class="mini circle icon"></i> <%= time_ago_in_words(card.created_at) %> ago</span>
      </div>
      <div class="description">
       <%= markdown(card.description) %>
     </div>
   </div>
   <% if current_user %>
   <div class="extra content">
     <% card.tags.each do |t| %>
       <a class="ui tag label"><%= t.name %></a>
     <% end %>
     <span class="right floated">
      <div id="likes_<%= card.id %>">
        <%= render 'likes', card: card %>
      </div>
     </span>
    </div>
  <% end %>
  </div>
<% end %>
</div>
