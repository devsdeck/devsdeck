  <%= link_to flashcard_like_path(flashcard), method: :patch, remote: true do %>
  <% if current_user.voted_for? flashcard %>
<i class="heart red like icon"></i>
    <% else %>
<i class="heart outline like icon"></i>
  <% end%>
  <% end %>
  <%= flashcard.get_upvotes.size %> 
 
